<!DOCTYPE html>
<html>
  <head>
    <title>Gaijinfoodie</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <script src="https://kit.fontawesome.com/4f6c5edafa.js" crossorigin="anonymous"></script>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  </head>

  <body>
    <nav class="navbar bg-body-tertiary" data-bs-theme="dark">
      <div class="container-fluid d-flex px-5">
        <a class="navbar-brand" href="#">The Gaijin Foodie</a>
        <div class="justify-content-end">
          <% if current_user&.admin? %>
            <%= link_to button_data[:text], button_data[:path], class: 'btn btn-primary' %>
          <% end %>
          <% if user_signed_in? %>
            <%= link_to "Sign out", destroy_user_session_path, data: { turbo_method: :delete }, class: 'btn btn-danger mx-2' %>
          <% else %>
            <%= link_to "Sign in", new_user_session_path, class: 'btn btn-primary' %>
          <% end %>
        </div>
      </div>
    </nav>
    <%= yield %>
    <%= render "shared/footer" %>
  </body>
</html>
